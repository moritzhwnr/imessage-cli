# Copy to ~/.config/imessage-bridge/.env (or ./.env in the dir you run from)
# to override defaults. Values already exported in the shell environment win.

# Backend URL. Defaults to the production broker — only set this if you're
# running a local dev backend or pointing at staging.
# IMESSAGE_BRIDGE_BACKEND_URL=http://localhost:3000
